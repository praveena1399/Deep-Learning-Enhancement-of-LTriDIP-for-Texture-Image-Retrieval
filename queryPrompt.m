function query = queryPrompt()
%query = NaN;
%tot = 3; % how many attempts to make
%while isnan(query) && tot>0
%while tot>0
    query = input('Enter query image name :', 's');
    %tot = tot-1;
%end
end